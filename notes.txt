What worked:
1
- rm .venv -r ; python -m venv .venv ; source .venv/bin/activate
- pip install --upgrade "jax[cuda12_pip]" -f https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
- pip install dreamerv3

2
- conda env create --prefix .env -f environment.yml' (on 19/05/2023)

What did not work:
1
- rm .venv -r ; python -m venv .venv ; source .venv/bin/activate
- pip install --upgrade "jax[cuda12_pip]"
  `WARNING: jax 0.4.6 does not provide the extra 'cuda12_pip'`
  `ModuleNotFoundError: jax requires jaxlib to be installed.`

